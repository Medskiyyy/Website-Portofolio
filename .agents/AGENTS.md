# Workspace-Scoped Agent Rules

## Project Learnings

- Always verify the completion status of showcase projects (such as SaaS dashboards) before rendering them on active landing pages, and query local directories (like `C:\Users\Ahmad\HitungUang`) to extract accurate technology specs.
